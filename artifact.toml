kind = "web"
previewPath = "/"
title = "ملاعب - حجز الملاعب الرياضية"
version = "1.0.0"
id = "artifacts/stadium"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21628

[services.development]
run = "pnpm --filter @workspace/stadium run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/stadium", "run", "build" ]
publicDir = "artifacts/stadium/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21628"
BASE_PATH = "/"
